# GENERATED FILE: python3 scripts/ingress_contract.py --write
# Source: deployments/ingress/contract.json
events {}

http {
    client_max_body_size 1m;

    upstream backend { server backend:8000; }
    upstream frontend { server frontend:80; }
    upstream mcp { server mcp:9000; }
    upstream recorder { server recorder:8080; }

    server {
        listen 443 ssl default_server;
        server_name _;

        ssl_certificate /etc/nginx/certs/wren-e2e.pem;
        ssl_certificate_key /etc/nginx/certs/wren-e2e-key.pem;
        return 444;
    }

    server {
        listen 443 ssl;
        server_name app.wren.test;

        ssl_certificate /etc/nginx/certs/wren-e2e.pem;
        ssl_certificate_key /etc/nginx/certs/wren-e2e-key.pem;

        location = /_e2e/sentry/api/1/envelope/ {
            proxy_pass http://recorder;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
        }

        location = /_e2e/recorder/ready {
            proxy_pass http://recorder;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Recorder-Token $http_x_recorder_token;
        }

        location = /_e2e/recorder/envelopes {
            proxy_pass http://recorder;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Recorder-Token $http_x_recorder_token;
        }

        location = /_e2e/recorder/artifacts {
            proxy_pass http://recorder;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Recorder-Token $http_x_recorder_token;
        }

        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
        }

    }

    server {
        listen 443 ssl;
        server_name api.wren.test;

        ssl_certificate /etc/nginx/certs/wren-e2e.pem;
        ssl_certificate_key /etc/nginx/certs/wren-e2e-key.pem;

        location ~ ^/(metrics|healthz|readyz)/?$ {
            return 404;
        }

        location / {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
        }

    }

    server {
        listen 443 ssl;
        server_name mcp.wren.test;

        ssl_certificate /etc/nginx/certs/wren-e2e.pem;
        ssl_certificate_key /etc/nginx/certs/wren-e2e-key.pem;

        location = /.well-known/oauth-protected-resource {
            proxy_pass http://mcp;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
        }

        location = /mcp {
            proxy_pass http://mcp;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
        }

        location ^~ /mcp/ {
            proxy_pass http://mcp;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto https;
        }

        location / {
            return 404;
        }

    }

}
