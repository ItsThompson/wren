# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# --- Node / frontend --------------------------------------------------------
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.turbo/

# --- Environment (secrets; .env.example is committed) -----------------------
.env
.env.local
.env.*.local
e2e/.env.test
# .env.prod is intentionally COMMITTED (non-secret production config; the .env
# rule above does not match it and .env.*.local needs a .local suffix). Secrets
# stay in GitHub Actions, never here.
!.env.prod

# --- Cloudflare tunnel credentials and rendered configs ---------------------
deployments/cloudflare/*.json
deployments/cloudflare/*.rendered.yml
deployments/cloudflare/cert.pem

# --- Alertmanager rendered config (holds the real Discord webhook) ----------
deployments/alertmanager/*.rendered.yml

# --- IDE / OS ---------------------------------------------------------------
.idea/
.vscode/
.DS_Store
Thumbs.db

# --- Playwright -------------------------------------------------------------
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
